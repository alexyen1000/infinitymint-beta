import { InfinityMintWindow } from "../window";

const Imports = new InfinityMintWindow(
	"Imports",
	{
		fg: "white",
		bg: "black",
		border: {
			fg: "#f0f0f0",
		},
	},
	{
		type: "line",
	}
);

export default Imports;
